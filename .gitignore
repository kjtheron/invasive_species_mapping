# Python / uv
.venv/
__pycache__/
*.py[cod]
.ruff_cache/
.pytest_cache/

# Secrets
.env

# Pipeline artifacts (local-first; not committed) — but keep dataset READMEs,
# since the data itself can't be redistributed (must be re-downloaded).
data/**
!data/**/
!data/**/README.md
logs/
src/inspo/

# Data sourcing temp
dataset_lookup.csv
dataset_lookup_international.csv

# Project roadmaps
tasks/
CLAUDE.md

# nightly chip window lock
.chip_window.lock
